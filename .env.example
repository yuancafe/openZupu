# Database — PostgreSQL (matches production schema; both local dev and Render use Postgres)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/openzupu?schema=public"

# JWT
JWT_SECRET="change-me-to-a-random-64-char-string"

# Server
PORT=3001
